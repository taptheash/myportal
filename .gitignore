# Repo-wide safety net. portal/.gitignore only covers portal/, so any new
# top-level folder (like the old mobile/) could commit secrets or deps.
node_modules/
build/
.env
.env.*
!.env.example
.vercel/
.DS_Store
